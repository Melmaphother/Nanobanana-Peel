# 🍌 Nanobanana Peel (剥香蕉皮) - AI 生成图片去背景工具

🌐 **在线体验：** https://melmaphother.github.io/nanobanana-peel/

一个轻量级的 Python 工具，用于去除 AI 生成图片的背景，添加透明通道。

[English](README.md) | 简体中文

## 🎯 为什么需要这个工具？

AI 模型生成的图片（如 GPT-4o、Gemini、Claude、Grok、DALL-E 3、Midjourney、Flux、Stable Diffusion，甚至最新的 Nano Banana Pro 🍌）经常有两个问题：

1. **背景不是纯白色**：带有轻微的泛黄或灰白色调，粘贴到文档、PPT 时很丑
2. **PNG 没有透明通道**：即使背景看起来是"白色"，也是 RGB 格式，粘贴到其他地方会造成遮挡

这个工具可以一次性解决这两个问题：
- 检测接近背景色的像素，将它们变成完全透明
- 输出带有 Alpha 通道的干净 PNG

## ✨ 效果对比

| 原始图片 | 处理后 |
|:-------:|:------:|
| ![原始](assets/1.png) | ![处理后](assets/1_rmbg.png) |

> 左：原始图片有轻微的灰色背景。右：干净的透明背景图片。

## 📦 安装

### 环境要求

- Python 3.9+
- NumPy
- Pillow

### 安装依赖

```bash
pip install numpy pillow
```

### 克隆仓库

```bash
git clone https://github.com/Melmaphother/Nanobanana-Peel.git
cd Nanobanana-Peel
```

## 🚀 使用方法

### 基础用法

```bash
# 处理单个图片（输出: input_rmbg.png）
python rmbg.py -i input.png

# 指定输出路径
python rmbg.py -i input.png -o output.png
```

### 批量处理

```bash
# 一次处理多个图片
python rmbg.py -i image1.png image2.png image3.png

# 输出文件: image1_rmbg.png, image2_rmbg.png, image3_rmbg.png
```

### 自定义阈值

```bash
# 更激进的背景去除（更低的阈值）
python rmbg.py -i input.png --transparent 0.05

# 半透明过渡区域
python rmbg.py -i input.png --transparent 0.05 --opaque 0.3
```

### 去除非白色背景

```bash
# 去除黑色背景
python rmbg.py -i input.png --background 0,0,0

# 去除自定义颜色背景 (RGB)
python rmbg.py -i input.png --background 128,128,128
```

## ⚙️ 参数说明

| 参数 | 默认值 | 说明 |
|-----|-------|------|
| `-i, --input` | (必需) | 输入图片路径，支持多个文件进行批量处理 |
| `-o, --output` | `<输入文件名>_rmbg.png` | 输出图片路径。仅在处理单个文件时有效。如不指定，输出文件将保存为 `<输入文件名>_rmbg.png` |
| `--transparent` | `0.1` | 透明阈值 (0-1)。颜色距离低于此值的像素变为完全透明 |
| `--opaque` | `1.0` | 不透明阈值 (0-1)。颜色距离高于此值的像素保持完全不透明 |
| `--background` | `255,255,255` | 要检测的背景色，格式为 `R,G,B` |

### 理解阈值

工具会计算每个像素与指定背景色之间的"颜色距离"：

- **距离 = 0**：像素与背景色完全相同
- **距离 = 1**：像素与背景色的距离达到最大值

使用默认设置（`--transparent 0.1`）：
- 与白色距离在 10% 以内的像素 → **透明**
- 其他所有像素 → **保持原样**

## 🔧 全局安装（添加到 Shell）

想要在任何地方都能使用 `rmbg` 命令？把它添加到你的 Shell 配置中：

### Zsh 用户 (~/.zshrc)

```bash
# 添加这行到你的 ~/.zshrc
alias rmbg="python /path/to/Nanobanana-Peel/rmbg.py"

# 然后重新加载配置
source ~/.zshrc
```

### Bash 用户 (~/.bashrc)

```bash
# 添加这行到你的 ~/.bashrc
alias rmbg="python /path/to/Nanobanana-Peel/rmbg.py"

# 然后重新加载配置
source ~/.bashrc
```

现在你可以在任何地方使用：

```bash
# 单文件处理（自动生成 ai_image_rmbg.png）
rmbg -i ~/Downloads/ai_image.png

# 指定输出路径
rmbg -i ~/Downloads/ai_image.png -o ~/Downloads/ai_image_transparent.png

# 批量处理
rmbg -i ~/Downloads/*.png
```

## 📐 工作原理

1. **加载图片**：读取输入的 PNG 文件
2. **计算距离**：对每个像素，计算它与背景色在 RGB 空间中的欧几里得距离
3. **归一化**：将距离归一化到 [0, 1] 范围（RGB 最大距离 ≈ 441.67）
4. **应用透明度**：
   - 距离 < `transparent_threshold` → Alpha = 0（完全透明）
   - 距离 > `opaque_threshold` → Alpha = 255（完全不透明）
   - 介于两者之间 → 保持原始 Alpha
5. **保存**：输出带有 Alpha 通道的 PNG

## 📝 使用示例

```bash
# 1. 下载一张 AI 生成的图片
# 2. 去除背景（自动生成 ai_generated_rmbg.png）
rmbg -i ai_generated.png

# 3. 对于黄色背景更明显的图片，使用更低的阈值
rmbg -i yellow_bg.png --transparent 0.15

# 4. 批量处理文件夹中的所有图片
rmbg -i *.png
```

## 📄 许可证

MIT License - 随意使用和修改。

## 🤝 贡献

欢迎提 Issue 和 Pull Request！

